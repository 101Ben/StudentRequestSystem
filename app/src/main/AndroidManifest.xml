<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.slc">

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Navigator" />
        <activity android:name=".Workshops" />
        <activity android:name=".HsBioChem" />
        <activity android:name=".WorldLanguages" />
        <activity android:name=".Writing" />
        <activity android:name=".AccountingAndBusiness" />
        <activity android:name=".MathAndScience" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".Infomation" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".DashboardDisplay" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>